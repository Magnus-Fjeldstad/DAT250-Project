# Run Instructions

## Backend

From the project root:

```sh
docker compose build
docker compose up
```

Backend runs at http://localhost:8080

## Frontend

From poll-frontend

```sh
cd poll-frontend
npm install #Only when there is changes
npm run dev
```

Frontend runs at http://localhost:5173

## Notes

Production server setup for Axios will be added later.